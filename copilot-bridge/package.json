{
  "name": "benchmark-qed-copilot-bridge",
  "private": true,
  "version": "0.0.1",
  "description": "HTTP/SSE bridge that hosts a @github/copilot-sdk session per UI conversation and forwards ask_user / elicitation / permission requests to the browser.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs"
  },
  "dependencies": {
    "@github/copilot-sdk": "^1.0.0-beta.4"
  },
  "engines": {
    "node": ">=20"
  }
}
